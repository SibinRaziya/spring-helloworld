name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
           login-server: knowledgefactorydev.azurecr.io
           username: 
           password: ihtIRjERWAme2KQEywFLtECz=7
      - run: |
          docker build . -t knowledgefactorydev.azurecr.io/image:latest
          docker push knowledgefactorydev.azurecr.io/image:latest
